# Copy this file to .env and fill in the real value. .env is git-ignored and
# is never read by anything except the scripts in this repo -- do not paste
# the real key anywhere else (chat, commit messages, other files).
NEURONPEDIA_API_KEY=
