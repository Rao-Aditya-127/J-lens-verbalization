.env
dataset/jlens/raw*/
*.jsonl
think/
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
.DS_Store
Thumbs.db
# Exploratory API scripts, most superseded. Two are kept: they produced the
# concept-swap result written up in results/concept-swap/, and git cannot
# re-include a file whose parent DIRECTORY is excluded, hence the /* form.
dataset/jlens/analysis/*
!dataset/jlens/analysis/injection_curve.py
!dataset/jlens/analysis/swap_report.py
report/

# Claude Code local machine state (settings.json, if added, stays tracked)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# scratch / backup artefacts
*.bak
*.tmp
nohup.out

# Training outputs: adapters, checkpoints and downloaded run results.
# results_day1/ alone is 647 MB -- these are artefacts, the repo stays code-only.
results_day*/
training/runs/
training/data/
training/eval_*.json
eval_*.json
training/train.log
*.safetensors
*.bin
inject_*.json
training/inject_*.json
training/jlens_shift.json

# Probe run logs. The tallies that matter are transcribed into
# training/analysis/plot_regression.py; the raw stdout is an artefact.
base30.txt
probe_*.txt
*.log
injection_curve_results.json
